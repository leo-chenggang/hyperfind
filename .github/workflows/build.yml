name: Build HyperFind

on:
  push:
    branches: [main, master]
  workflow_dispatch:  # 允许手动触发

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # uvloop is macOS/Linux only — strip it on Windows
          (Get-Content requirements.txt) -replace 'uvloop.*', '' | Set-Content requirements-win.txt
          pip install -r requirements-win.txt

      - name: Build with PyInstaller
        run: |
          python -m PyInstaller app.spec --clean --noconfirm

      - name: Upload HyperFind.exe artifact
        uses: actions/upload-artifact@v4
        with:
          name: HyperFind-Windows
          path: dist/HyperFind.exe
          retention-days: 90
